version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: coffeeshop_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ta_db_coffeeshop
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  backend:
    build: .
    container_name: coffeeshop_backend
    restart: always
    ports:
      - "8083:8083"
    environment:
      PORT: 8083
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/ta_db_coffeeshop?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: rootpassword
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    depends_on:
      db:
        condition: service_healthy

  web:
    build:
      context: ../../xampp/htdocs/ta-ci4-web-coffeeshop
      dockerfile: Dockerfile
    container_name: coffeeshop_web
    restart: always
    ports:
      - "8080:80"
    environment:
      CI_ENVIRONMENT: development
      database.default.hostname: db
      database.default.database: ta_db_coffeeshop
      database.default.username: root
      database.default.password: rootpassword
      database.default.DBDriver: MySQLi
    depends_on:
      db:
        condition: service_healthy

volumes:
  mysql_data:
