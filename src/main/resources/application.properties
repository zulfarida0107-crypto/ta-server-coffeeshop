spring.application.name=server

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/ta_db_coffeeshop?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=${PORT:8083}

# JWT Configuration
jwt.secret=kopi-kenangan-petang-secret-key-2026-very-secure-key-that-is-long-enough-for-hs256
jwt.expiration=86400000

# Mail Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=zulfarida0107@gmail.com
spring.mail.password=zulfarida_app_password_placeholder
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Actuator Configuration
management.health.mail.enabled=false
management.endpoint.health.show-details=always


